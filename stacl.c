#include<stdio.h>
#include<stdlib.h>
#define [MAX_SIZE] 10
int stack[MAX_SIZE],top=-1;
int isFull(){
	if(MAX_SIZE==
}
int is Empty() {
	return (top==MAX_SIZE-1?1:-1)
}
int peek(){

	return stack[top];
}

void push(int e){
	 
	if(!isFull ())
	{
	}
	else{
}
void pop(){
	int d;
	if(!isEmpty())
	{
